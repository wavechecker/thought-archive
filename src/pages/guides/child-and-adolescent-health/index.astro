---
import HeroPage from "@/components/HeroPage.astro";
import { getHeroPageConfig } from "@/lib/hero-pages";

const config = getHeroPageConfig("child-and-adolescent-health");
if (!config) throw new Error("Hero page config not found: child-and-adolescent-health");
---

<HeroPage config={config} />

---
import HeroPage from "@/components/HeroPage.astro";
import { getHeroPageConfig } from "@/lib/hero-pages";

const config = getHeroPageConfig("infectious-diseases");
if (!config) throw new Error("Hero page config not found: infectious-diseases");
---

<HeroPage config={config} />

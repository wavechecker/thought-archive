---
import { getEntryBySlug } from "astro:content";

const { slug } = Astro.params;
const guide = await getEntryBySlug("guides", slug);
if (!guide) { throw new Error(`Guide not found for slug: ${slug}`); }

const { Content, data } = guide;

// Inline helpers (no imports = no resolve errors)
const isValidDate = (d) =>
  d instanceof Date && !isNaN(+d) && +d > +new Date("1971-01-01");
const fmt = (d) =>
  new Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "short", year: "numeric" }).format(d);

const displayDate = isValidDate(data.publishDate)
  ? data.publishDate
  : (isValidDate(data.updatedDate) ? data.updatedDate : undefined);
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{data.title} • PatientGuide</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    <article>
      <h1>{data.title}</h1>
      {displayDate && (
        <p><time datetime={displayDate.toISOString()}>{fmt(displayDate)}</time></p>
      )}
      <Content />
    </article>
  </body>
</html>



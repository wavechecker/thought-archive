---
const home = "/guides/";
const title = "Page not found";
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title} • PatientGuide</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- keep 404s out of search indexes -->
    <meta name="robots" content="noindex" />
    <style>
      :root { color-scheme: light dark; }
      body {
        margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
        display: grid; place-items: center; min-height: 100vh; padding: 2rem;
      }
      .card {
        max-width: 720px; width: 100%;
        border: 1px solid color-mix(in oklab, CanvasText 15%, Canvas);
        border-radius: 16px; padding: 2rem;
        box-shadow: 0 4px 24px color-mix(in oklab, CanvasText 10%, transparent);
      }
      h1 { margin: 0 0 .5rem 0; font-size: clamp(1.5rem, 2.5vw + 1rem, 2.2rem); }
      p { margin: .25rem 0 1rem; line-height: 1.6; }
      .links { display: flex; gap: .75rem; flex-wrap: wrap; }
      a {
        text-decoration: none; padding: .6rem .9rem; border-radius: 999px;
        border: 1px solid color-mix(in oklab, CanvasText 15%, Canvas);
      }
      a:hover { background: color-mix(in oklab, CanvasText 6%, Canvas); }
      code { background: color-mix(in oklab, CanvasText 6%, Canvas); padding: .2rem .4rem; border-radius: 6px; }
    </style>
  </head>
  <body>
    <main class="card">
      <h1>404 — Page not found</h1>
      <p>We couldn’t find that page. Try our guides index or check the URL.</p>
      <div class="links">
        <a href={home}>Go to Guides →</a>
        <a href="/">Home</a>
      </div>
      <p style="margin-top:1rem; opacity:.7;">
        If you typed the address, double‑check for typos — e.g. <code>/guides/</code>.
      </p>
    </main>
  </body>
</html>
